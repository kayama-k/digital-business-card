# 開発ルール

第1段階の公開名刺は、公開事故を防ぐための最小ハーネスで運用する。

## 必須の自動チェック

- `npm run verify` は、公開情報チェック、Lint／Formatter、本番ビルド、主要導線のPlaywright E2Eを実行する。
- `main` へ公開する成果物は、GitHub Actions上の `npm run verify` が成功したものだけにする。
- 変更した利用者向け挙動には、最も小さい適切な自動テストを追加または更新する。
- TypeScriptのstrict設定、未使用コード検出、BiomeのLint／Formatter、`.editorconfig` の整形規約を維持する。`npm run format` による機械的な整形は許可するが、意味のある構造変更は自動適用しない。

## 保守性と安全性

- 文言、公開URL、素材パス、アニメーション値は `src/config/card.ts` を唯一の参照元とする。
- GitHub Pagesは公開環境である。トークン、鍵、パスワード、接続文字列、秘密の `VITE_*` 変数を追加しない。`npm run check:public` が検知した場合は公開しない。
- アクセシビリティ、共有・保存の失敗時の代替経路、`prefers-reduced-motion` は利用者向け変更の受け入れ条件に含める。
- 機能変更、見た目・素材の変更、リファクタリング、依存更新は、可能な限り別の変更にする。

## 最小コードレビュー

通常の変更は自動チェックで完結する。GitHub Actions／Pages／公開URL／秘密情報／外部連携／大規模リファクタリング、または検証結果に不明点がある変更だけは、`docs/code-review.md` に沿う追加レビューを行う。追加レビューは短いリスク確認であり、日常変更の必須ゲートにはしない。
